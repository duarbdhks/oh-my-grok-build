# 로드맵

[English](roadmap.md)

## v0.1 — 콘텐츠형 프리뷰

- 마켓플레이스와 플러그인 매니페스트
- 계획·실행·병렬·검증·워크플로·진단 스킬
- 역할별 에이전트 6개
- 정적 검증과 CI
- 훅·MCP·데몬·외부 상태 저장 없음

## v0.1 검증 게이트

안정 릴리스 전에 다음 실제 세션 시나리오를 통과해야 합니다.

1. ✅ GitHub 마켓플레이스 추가와 설치·활성화·재로드
2. ✅ `/ogb-doctor`가 스킬 6개와 에이전트 6개를 확인
3. ✅ `/ogb-plan`이 소스를 수정하지 않고 saved plan을 생성
4. ✅ `/ogb-start`가 두 개의 독립 worktree 작업을 통합
5. ⬜ 충돌하는 파일 소유권을 실행 전에 차단 — 소유권이 겹치는 시나리오를 아직 만들지 않음
6. ✅ 실패한 테스트를 성공으로 오판하지 않음
7. ✅ `oh-my-grok-build:verifier`와 `check-work`가 독립적으로 실행
8. ✅ `/ogb-workflow` 스크립트 본문이 `validate_only`를 통과하고 라이브 실행 1회를 완료했으며, 인자 누락을 에이전트 시작 전에 차단

증거는 `docs/validation.ko.md`에 있습니다. 번호가 붙은 시나리오 중 5번만 미확인이며, 이는 충돌하는 작업 분할을 의도적으로 만들어야 재현됩니다. `script_path`를 통한 저장 프로젝트 워크플로 로드에는 별도의 folder trust 한계가 남아 있고 같은 문서에 기록했습니다.

## v0.2 — 검증된 워크플로 레시피

실제 Grok CLI에서 스모크 테스트한 후 다음 Rhai 워크플로 후보를 추가합니다.

- 변경 파일별 병렬 리뷰
- 모듈별 테스트 진단
- 계획 검토 패널
- 마이그레이션 preflight

정적 예제는 Grok Build의 workflow 인터페이스가 안정될 때만 배포합니다.

## v0.3 — 팀 정책

- 조직용 managed configuration 예제
- SHA 고정 마켓플레이스 배포 예제
- 비용 예산 정책과 고위험 작업 정책 템플릿
- 팀별 AGENTS.md 연동 가이드

## v1.0 조건

- 세 개 이상의 실제 프로젝트에서 사용
- Linux와 macOS 설치·worktree 검증
- Grok Build 두 개 연속 릴리스에서 매니페스트 호환
- 고위험 작업의 계획 정지와 검증 실패 처리가 재현 가능
- 공개 명령의 입력·출력 계약 고정
